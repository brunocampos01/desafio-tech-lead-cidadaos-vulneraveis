# Backend tool config only. Runtime deps are pinned in ../requirements.txt (repo root).
# keep the ruff config

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
